pipeline {
  agents any
    stages {
      stage ('build') {
        steps {
               sh 'mvn clean install'
              }
             }
       stage ('deploy') {
        steps { 
            echo "deploy"
            }
          }           
    }
}
